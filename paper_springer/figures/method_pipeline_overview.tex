\begin{tikzpicture}[
  font=\sffamily\small,
  flow/.style={-Latex, line width=0.85pt, draw=black!75},
  stage/.style={
    draw=black!75,
    line width=0.75pt,
    rounded corners=2pt,
    fill=white,
    minimum width=2.2cm,
    minimum height=1.55cm,
    text width=1.95cm,
    align=center,
    inner sep=4pt
  },
  source/.style={stage, draw=blue!55, fill=blue!5},
  target/.style={stage, draw=orange!70!black, fill=orange!8},
  model/.style={stage, draw=green!45!black, fill=green!7},
  output/.style={stage, draw=black!45, fill=black!3}
]

\node[source] (inputs) at (0,0)
  {\textbf{Data integration}\\311, NTA, NOAA\\OSM, PLUTO};

\node[target] (panel) at (2.45,0)
  {\textbf{Panel and target}\\NTA-week-category\\shifted history\\next-week label};

\node[stage] (split) at (4.90,0)
  {\textbf{Temporal split}\\train 2015-2022\\validation 2023\\test 2024-2025};

\node[model] (model) at (7.35,0)
  {\textbf{Modeling}\\LightGBM\\XGBoost\\ensemble};

\node[output] (decision) at (9.80,0)
  {\textbf{Decision layer}\\category thresholds\\alerts\\SHAP review};

\draw[flow] (inputs.east) -- (panel.west);
\draw[flow] (panel.east) -- (split.west);
\draw[flow] (split.east) -- (model.west);
\draw[flow] (model.east) -- (decision.west);

\end{tikzpicture}
