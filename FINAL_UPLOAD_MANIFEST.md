# Final Upload Manifest

Upload one reviewer-facing file:

- `paper_springer/main_SIMC_submission.pdf` - self-contained SIMC-compliant manuscript PDF.

Optional source package, only if requested by the venue:

- `paper_springer/main.tex`
- `paper_springer/main_SIMC_submission.tex`
- `paper_springer/references.bib`
- `paper_springer/figures/`

Repository diagnostic artifacts are not separate submission material. They support reproducibility through the public repository.

The final branch-tip SHA is recorded in the Git delivery response and GitHub history; a commit cannot embed its own final SHA without changing it.
